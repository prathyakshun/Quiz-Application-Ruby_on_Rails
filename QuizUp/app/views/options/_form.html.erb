<%= form_with(model: option, local: true) do |form| %>
  <% if option.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(option.errors.count, "error") %> prohibited this option from being saved:</h2>

      <ul>
      <% option.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :optiontext %>
    <%= form.text_area :optiontext, id: :option_optiontext %>
  </div>

  <div class="field">
    <%= form.label :Question_id %>
    <%= form.text_field :Question_id, id: :option_Question_id %>
  </div>

  <div class="field">
    <%= form.label :correct %>
    <%= form.check_box :correct, id: :option_correct %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
