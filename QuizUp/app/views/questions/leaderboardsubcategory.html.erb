<!DOCTYPE html>
<html>
<body>

<h1 style="color:green">Choose Subgenre</h1>
<br />

<style>
table {
    font-family: arial, sans-serif;
    border-collapse: collapse;
    width: 100%;
}

td, th {
    border: 1px solid #dddddd;
    text-align: left;
    padding: 8px;
}

tr:nth-child(even) {
    background-color:  #f1c9a1 ;
}
tr:nth-child(odd) {
  background-color:  #f1f37f ;
}
</style>

<%= form_tag do %>
<h2 style="color:blue; text-align:left; font-size:200%">Subcategory</h2>
<%= select_tag(:subcategorydrop, options_for_select(@subcategoryarr), {:class => "form-control"}) %>

<br />
<button class="btn btn-primary"> submit </button>
<% end %>
<br /><br />
<table class="table table-hover">
	<tr>
		<th>Rank </th>
		<th>User Email </th>
		<th>Score</th>
	</tr>
	<% i=1 %>
	<% @myhasharr.each do |d| %>
		<tr>
			<td> <%= i %> </td>
  			<td> <%= User.find(d[1]).email %> </td>
  			<td> <%= d[0] %></td>
  			<% i=i+1 %>
  		</tr>
	<% end  %>
</table>

<%= link_to "Go to Genre", leaderboard_path, {:style=>'color:#FFFFFF;',:class => "btn btn-success"} %>
<%= link_to "Go to main page", landingpage_path, {:style=>'color:#FFFFFF;',:class => "btn btn-info"} %>
</body>
</html>